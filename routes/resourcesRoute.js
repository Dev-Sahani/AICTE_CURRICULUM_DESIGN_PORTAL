const express = require("express");
const router = express.Router();


router.route("/get-resouces") // wiht search functionality 
router.route("/add-resource")
router.route("/delete-resouce")

module.exports = router;