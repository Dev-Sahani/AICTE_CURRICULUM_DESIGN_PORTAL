const express = require("express");
const router = express.Router();


router.route("/get-changes") 
router.route("/add-change")
router.route("/delete-change") 
router.route("/apply-changes")

module.exports = router;